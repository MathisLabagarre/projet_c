{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C Project",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "args": ["-L", "./storage/hash.txt", "-H", "md5"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "build project",
            "MIMode": "gdb"
        }
    ]
}
